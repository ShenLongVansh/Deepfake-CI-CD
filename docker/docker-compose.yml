services:
  api:
    image: shenlongcodes/voiceguard-inference:latest
    ports:
      - "8000:8000"
    volumes:
      - /opt/models/deepfake:/opt/models/deepfake
    environment:
      - MODEL_PATH=/opt/models/deepfake
